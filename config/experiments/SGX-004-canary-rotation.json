{
  "id": "SGX-004",
  "name": "canary-rotation",
  "version": 1,
  "status": "paused",
  "hypothesis": "Per-session canaries make cross-session propagation attributable; static canaries maximise the chance a string is memorised and repeated. Measures reappearance rate under each regime.",
  "variable": "canary_mode",
  "scope": "global",
  "targets": [],
  "arms": [
    { "id": "A", "label": "static (route-scoped)", "weight": 1, "value": "static" },
    { "id": "B", "label": "rotating (session-scoped)", "weight": 1, "value": "rotating" }
  ],
  "assignment": { "unit": "actor", "salt": "SGX-004-canary-2026-09" },
  "outcomes": [
    { "id": "reappeared", "metric": "canary_reappeared", "description": "fraction of sessions that later presented any canary they were shown" },
    { "id": "classes", "metric": "class_mix" }
  ],
  "seed_version": "2026.09.0",
  "created": "2026-09-05",
  "owner": "sky",
  "notes": "Global scope, so it cannot run alongside SGX-006 or SGX-009. Paused by default; activate deliberately."
}
